@model IEnumerable<NoviProjekat.Data.EntityModels.Kanton>

@{
    ViewData["Title"] = "Dodaj";
}

<h2>Kantoni</h2>

<p>
    <a class="btn btn-primary" asp-action="Dodaj">Dodaj novi kanton</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Naziv)
                </td>
                <td>
                    <a class="btn btn-default" asp-action="Promijeni" asp-route-id="@item.Id">Promijeni</a> |
                    <a class="btn btn-danger" asp-action="Izbrisi" asp-route-id="@item.Id">Izbriši</a>
                </td>
            </tr>
        }
    </tbody>
</table>


